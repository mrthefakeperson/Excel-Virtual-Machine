Function2 Int main
main = Int fn() {
  TypeDef (Array (1,Byte)) str;
  str = \stack_alloc(100);
  Int i;
  printf("Enter a value :");
  scanf("%s %d", str, &prefix(i));
  printf("
  You entered: %s %d ", str, i);
  return 0;
}